import React from "react"
import Blob from "../secondary/Blob"
import ProjectCarousel from "../secondary/ProjectsCarousel"

const ProjectsIndex = props => (
  <div id="projects-index" className="banner">
    <Blob />
    <ProjectCarousel />
  </div>
)

export default ProjectsIndex
